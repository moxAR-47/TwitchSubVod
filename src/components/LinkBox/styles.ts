import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  flex-direction: column;
  align-items: bottom;
  text-align: center;
  margin-top: 1rem;

  svg {
    margin-right: 0.5rem;
  }
  a {
    color: var(--text);
  }
`;
